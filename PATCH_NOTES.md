# 우공실 변경 이력

이 문서는 사용자가 체감하는 기능 변화와 운영 안정성 개선을 날짜별로 정리한 기록입니다. 내부 서버 키, 운영 DB 덤프, 개인 백업 경로, 비밀 환경 변수는 포함하지 않습니다.

## 2026-09-10

### v2.4.5 — 계정·게시글·첨부·채점 보안과 운영 권한 보완

이메일 인증을 계정·용도·브라우저에 연결하고, 회원 로그인을 HttpOnly 쿠키와 만료·폐기 가능한 서버 세션으로 전환했습니다. 공지 관리에는 관리자 이메일 OTP 인증을 적용하고, 게시글 종류·첨부 조회 권한을 서버에서 확인합니다. SVG와 위장 이미지 업로드를 거절하며, 시험은 서버가 발급한 응시에서 답안 제출 후 채점합니다. 반복 제출이나 브라우저의 점수 변경으로 기록을 만들 수 없습니다.

조회 범위·요청 제한, CSP·HSTS·운영 출처 제한, 최소 DB 실행 권한, 민감 원문 없는 로그와 보존량 제한을 반영했습니다. 서버 접근 보완은 시간 제한 자동 복구와 새 SSH 연결 확인을 포함합니다. 기존 서버를 사용하며 증설·AWS 스냅샷 추가는 수행하지 않습니다.

로컬 최종 백엔드 **172/172**, 프런트엔드 **23/23**, 실제 Node 22·MySQL·모바일 브라우저 **105/105**를 통과했습니다. 양쪽 전체 패키지 감사는 **0건**, 웹 Lint·빌드와 배포·복구 검사를 통과했습니다. Android 후보는 **16/16**, 빌드·서명 대조를 통과했고 Lint 오류 0개·경고 5개가 남았습니다. 실제 운영 배포·기기 설치 결과는 실행 뒤 [보안 보완 검증 문서](./docs/SECURITY_HARDENING_2.4.5.md)에 추가합니다.

## 2026-09-09

### v2.4.4 — 관리자 이메일 인증번호 2차 인증

`skn29`의 기존 사이트 비밀번호 확인 후 등록된 이메일로 6자리 인증번호를 보냅니다. 번호까지 확인해야 관리자 세션을 발급하며, 비밀번호만 확인한 상태와 2차 인증 없이 발급된 기존 관리자 세션으로는 관리자 API를 사용할 수 없습니다. 등록 이메일은 서버에서 결정하며 요청으로 수신자를 바꿀 수 없습니다.

인증번호는 5분 유효·1회 사용이며 60초 후 재전송할 수 있습니다. 15분간 최대 5회 발송, 오입력 5회 시 15분 대기 제한을 DB에 저장합니다. 재전송하면 이전 번호가 만료되고, 발송 실패 시 관리자 세션을 발급하지 않습니다. 새로고침 시 인증 대기를 복구하고, 비밀번호·이메일 변경 및 계정 정지도 확인합니다. 번호와 브라우저 토큰 원문은 DB·localStorage·sessionStorage·로그에 저장하지 않습니다.

로컬 백엔드 **144/144**, 프런트엔드 **22/22**, 배포·복구 검사 **8/8**, 웹 린트·빌드를 통과했습니다. 실제 임시 MySQL의 동시 요청·오입력 집계·이전 세션 테이블 전환과 320·412·1440px 밝은/어두운 테마의 브라우저 로그인 흐름 검사 **77/77**를 통과했습니다. 계정과 메일 수신은 격리된 시험 데이터입니다. 운영 등록 이메일·메일 설정과 SMTP 인증은 확인했으며, 실제 받은편지함 수신과 운영 비밀번호를 사용하는 최종 로그인은 별도 확인 범위입니다. Android APK를 새로 빌드하지 않았습니다.

### v2.4.3 — 관리자 인증서 제한 해제와 skn29 로그인 유지

기기 인증서 제한을 취소하고 누구나 `/manage` 로그인 화면을 열 수 있도록 복원했습니다. 관리자 로그인은 `skn29` 계정만 허용하며 비밀번호·관리자 권한·정지 여부를 확인합니다. 비로그인 관리자 API 차단, 다른 관리자 계정의 이전 세션 폐기, 세션 만료·로그아웃, Origin·CSRF와 로그인 요청 제한은 유지합니다. 앞서 정리한 홈·하단 메뉴와 방문 횟수는 변경하지 않았습니다.

로컬 백엔드 **131/131**, 프런트엔드 **22/22**, 배포·복구 검사 **8/8**, 웹 린트·빌드를 통과했습니다. 인증서 없는 실제 로컬 HTTP 로그인과 Vite·Express의 관리자 HTML 경로, 412·1440px 브라우저 검사 **23/23**도 통과했습니다. 로그인 시험은 실제 bcrypt와 격리된 메모리 저장소를 사용했으며, 운영 DB·운영 비밀번호나 실제 휴대폰에서의 로그인 결과는 아닙니다. 삭제한 인증서 기능의 전용 검사 6개는 현재 백엔드 검사 수에서 제외했습니다. 빌드에는 오류 없이 플러그인 실행 시간 안내가 남았습니다.

운영 전환은 정식 릴리즈 배포와 Nginx의 클라이언트 인증서 요청·전달 설정 제거를 함께 확인합니다. 정상 실행은 기기 정책 파일을 요구하지 않으며, 이전 2.4.2 복구 검사만 기존 서버 내부 파일을 사용할 수 있습니다. [관리자 접근 안내](./docs/ADMIN_DEVICE_ACCESS.md)에 정책과 검증 범위를 기록합니다. Android APK는 이번 변경 대상이 아닙니다.

### v2.4.2 — 관리자 기기 인증과 홈 중복 항목 정리

공개 메뉴의 관리자 링크를 제거했습니다. `/manage` 직접 접속 시 승인된 두 기기의 TLS 인증서부터 확인하고, 인증서가 없거나 승인되지 않았으면 로그인 HTML과 관리자 API를 404로 차단합니다. 승인 기기에서도 `skn29` 계정과 기존 비밀번호·권한 검사가 필요하며, 다른 관리자 계정의 이전 세션도 사용할 수 없습니다. 기존 세션 만료·폐기와 Origin·CSRF 검사는 유지합니다.

하단의 `오늘의 운세`, `한국시간 기준` 문구와 홈의 보조 버튼을 제거했습니다. 홈의 시작 버튼은 `학습 시작하기` 하나로 통일했습니다. 상단 운세 메뉴, 하단 전체 이용 안내, 오늘·누적 방문 횟수와 한국시간 집계는 유지합니다.

로컬 백엔드 **137/137**, 프런트엔드 **22/22**, 배포·복구 검사 **8/8**, 웹 린트·빌드를 통과했습니다. 실제 로컬 Nginx HTTPS와 Express 관리자 경로 검사 **35/35**, 320·412·1440px의 밝은/어두운 테마·로그인 전후 화면 검사 **97/97**도 통과했습니다. HTTPS 검사는 격리된 시험용 저장소를 사용했고, 현재 노트북의 실제 TPM 인증서와 발급한 휴대폰 인증서의 통신을 확인했습니다. 이 결과는 휴대폰 Chrome의 설치·접속 완료를 대신하지 않습니다. 기기별 설정과 배포 후 확인은 [관리자 기기 접근 안내](./docs/ADMIN_DEVICE_ACCESS.md)에 구분합니다.

### v2.4.1 — 웹·APK 공통 하단 방문 통계

오늘 방문과 누적 방문을 공통 하단의 서비스 안내 아래에 표시합니다. 작은 휴대폰 화면에서는 자연스럽게 줄바꿈하며 고정 하단 메뉴와 겹치지 않습니다. 통계 연결 실패 때는 숫자 대신 대시를 표시하고 재접속 시 다시 조회합니다.

집계는 기존 5분 방문 세션과 과거 누적 기록을 유지합니다. 새로고침·화면 이동·동시 요청으로 같은 활성 방문을 중복 집계하지 않으며, 합계 조회 자체는 방문을 생성하지 않습니다. 한국시간 기준 방문 횟수이므로 `명` 대신 `회`를 사용합니다. 공개 API에는 합계 두 개만 포함하고 회원·개별 방문 기록은 관리자 전용으로 유지합니다.

로컬에서 백엔드 **129/129**, 프런트엔드 **22/22**, 실제 임시 MySQL의 HTTP·집계 검증 **12/12**, 배포·복구 검사 **6/6**을 통과했습니다. 웹 린트·프로덕션 빌드와 320·412·1440px 밝은/어두운 화면의 합성 브라우저 검사 **57/57**도 통과했습니다.

Android **2.4.1 debug APK** 생성과 단위검사 **16/16**, 오류 없는 Lint를 확인했습니다. 기존 Android 설정 관련 경고 4개는 남아 있습니다. 기본 APK는 운영 웹을 불러오고, 과거 웹 파일을 포함한 UI 테스트 APK는 새로 빌드·설치해야 최신 화면이 보입니다. 이번 로컬 확인 시 연결된 실제 Android 기기는 없어 실기기 재검증 결과를 추가하지 않았습니다. 자세한 범위는 [방문 통계 안내](./docs/VISITOR_COUNTS.md)에 기록합니다.

## 2026-09-08

### v2.4.0 — 공개 화면 정리와 GitHub 릴리즈 배포

일반 화면의 실시간 접속자 목록과 오늘·누적 방문 숫자를 제거했습니다. 일반 회원용 접속자 API도 종료했으며 방문 수집은 화면 표시 없이 유지하고 관리자 통계에서만 조회합니다. 사용자용 공지 응답에는 발송 당시 접속 인원도 포함하지 않습니다.

공지게시판은 실제 사용자에게 필요한 가이드를 유지합니다. 새로 작성한 운영자 개인정보 열람·방문 집계 설명 2개는 정리하고, 홈·모바일 업데이트 글은 v2.4.0으로 표시합니다. 과거 버전의 접속자 관련 설명에는 현재 제공하지 않는다는 안내를 붙여 당시 변경 이력을 보존합니다. 적용 전에 대상 원문과 댓글·추천 여부를 확인하고, 해당 글만 트랜잭션으로 변경합니다.

정식 GitHub 릴리즈 발행을 운영 배포의 시작점으로 구성했습니다. 검사·빌드, 파일 체크섬과 서버 소스 대조, 복구본·DB 백업, 운영 반영과 버전 확인을 수행합니다. 실패 시 변경 대상 소스·웹 화면과 이번 공지 변경을 되돌리도록 구성했습니다. 절차는 [릴리즈 배포 안내](./docs/RELEASE_DEPLOYMENT.md)에 기록합니다.

Android 기본 버전은 2.4.0, versionCode는 20400입니다. 이미 배포한 웹 묶음을 다시 빌드하지 않고 APK에 포함하는 `-WebDistRoot` 옵션과 버전·커밋·파일 해시 대조를 추가했습니다. 아래의 이전 기기·운영 검증 수치는 당시 결과이며 이번 버전의 검증 결과로 재사용하지 않습니다.

#### v2.4.0 배포와 검증 결과

- 소스 커밋 `f7c8a1d`를 `2026-09-08|공개 접속 정보 정리와 v2.4.0 릴리즈 자동 배포 구성`으로 푸시했습니다. [첫 정식 릴리즈](https://github.com/antisdream/woogongsil/releases/tag/v2.4.0)의 [GitHub Actions 실행](https://github.com/antisdream/woogongsil/actions/runs/34197393352)이 배포와 첨부 파일 등록까지 성공했고 운영 버전·커밋도 일치합니다.
- 최초 배포 전 앱·DB·운영 설정 전체 백업을 Lightsail에 저장하고 **24개 체크섬·DB 49테이블**을 검증했습니다. 자동 배포에서도 별도 DB 백업과 변경 대상 복구본을 생성했습니다.
- GitHub 배포 묶음 **324파일**, 운영 관리 대상 소스 **253파일**, 새 웹 파일 **65개**의 해시를 확인했습니다. 환경 파일·런타임 JSON·업로드·문제 이미지·폰트 등 보호 대상 **338파일**은 백업과 같았습니다. 백엔드는 한 번 재시작됐으며 확인 구간의 오류 로그 증가량은 **0바이트**였습니다.
- 공지는 **31→29개**, 전체 게시글은 **32→30개**입니다. 지정한 2개 삭제·5개 수정과 나머지 25개 글의 내용·고정 메타 보존을 확인했습니다. 댓글·답글·추천 관계의 개수는 같았습니다.
- 로컬 백엔드 **101개**, 공지 마이그레이션 **25개**, 프런트엔드 **18개**, 배포 보호·원복 **5개** 검사와 Lint·웹 빌드를 통과했습니다. 별도 MySQL 8.0.45에서 공지 31행 사본과 합성 관계로 실제 CLI 적용·재실행·원복·활동 보존·삭제 거부 **7단계**를 확인했습니다.
- 합성 브라우저 **75/75**, 운영 배포 화면 검사 **140/140**, 운영 화면 **28장**의 시각 검수를 통과했습니다. 운영 공지 화면은 읽기 전용 SQL 스냅샷을 사용했습니다. 기존 목록 API의 좋아요 갱신 쓰기를 피한 검사이므로 공개 공지 API 전체 E2E로 해석하지 않습니다.
- 최종 APK는 **2.4.0-mobile-ui / versionCode 20400**, 번들은 `mobile-ui-20260908T070551378Z`입니다. 포함 웹 **64파일**이 GitHub에서 배포한 파일과 같고, APK SHA-256은 `fdca1e6322325af15f6d4544b91d0dc9677890289817a01f082a2dafdcfad92f`입니다.
- Android 단위 검사 **16/16**, APK 빌드와 Lint를 통과했습니다. Lint 오류는 **0개**, 경고는 **5개**가 남아 있습니다(OldTargetApi 1, GradleDependency 2, ModifierParameter 1, ObsoleteSdkInt 1). Galaxy Z Flip4에 데이터 초기화 없이 업데이트했고 설치 APK 해시가 같습니다. 실제 WebView의 홈·FAQ·빈 비밀번호 입력란·뒤로가기·자산 대조 **15/15**를 확인했습니다.

기기 검사는 비회원 화면에 한정하며 회원 자격증명 입력·학습 저장·메일 발송은 수행하지 않았습니다. 검증용 네트워크 차단과 ADB 포워딩을 제거하고 앱을 정상 재실행했습니다. 기존 전역 애니메이션 설정은 유지했습니다. 이 결과는 Google Play 출시 준비 완료를 의미하지 않습니다.

### 홈 링크·QR 정리와 Android 시작 화면 보완

홈의 시험 접수·Notion·개발자·모바일 링크 묶음과 QR 창을 제거했습니다. 학습 시작, 홈페이지 SQL 예제, 이용 안내와 회원 접속 현황을 유지하고 휴대폰 안내는 같은 사이트 주소로 직접 접속하도록 정리했습니다. 이번 QR 변경은 홈의 표시·호출 코드 정리 범위입니다.

Android 앱은 본문 WebView를 미리 준비하고 문 열림과 페이드아웃을 포함한 약 2.95초의 오프닝을 보여줍니다. 전역 애니메이션 배율이 0인 기기에서도 이 앱의 시작 효과만 같은 속도로 재생하며 전역 설정은 변경하지 않습니다. 오프닝이 끝날 때까지 뒤의 본문으로 터치가 전달되지 않으며 Activity 재생성 때는 오프닝을 반복하지 않습니다.

#### Android 소스 첫 공개와 빌드 확인

- 공개 `android/` 디렉터리에 앱 소스·테스트·Gradle Wrapper와 선택 웹 번들 빌드 스크립트 **42개 파일**을 준비했습니다. SDK/JDK 개인 경로, local.properties, 환경 비밀값·서명 키·기기 식별자·빌드 산출물은 공개 대상에서 제외했습니다.
- Gradle daemon은 **JDK 25**, Java 바이트코드는 **17**, compileSdk는 **37**입니다. 기본 debug는 운영 웹을 불러오며, 공개 스크립트로 같은 저장소의 웹 화면을 포함한 APK도 생성할 수 있습니다. API·인증은 기존 HTTPS 서버에 연결합니다.
- 최종 APK는 공개 Android 소스와 웹 번들 빌드 스크립트에서 직접 생성했으며, 해당 공개 소스의 단위 검사 **16/16**, Lint와 debug 빌드를 통과했습니다. 최신 원본 네이티브 소스는 공개 복사본과 SHA-256이 같은 것으로 확인했습니다. 모든 환경에서 APK 해시가 같음을 보장하는 의미는 아닙니다.
- 홈 변경과 공개 Android 소스는 커밋 `028822a`에 포함했습니다. 빌드 명령과 서명·기기 검증 범위는 [Android 안내](./android/README.md)에 기록합니다.

#### 최종 APK와 실제 기기 확인

- 최종 확인 번들은 `mobile-ui-20260907T155718399Z`, APK SHA-256은 `4c848c5e4b9fa72259b085b68e80b990cfcd2470eafd69db0e9379aeb28d8616`입니다.
- APK의 웹 자산 **392개**가 서버에서 검증한 **522개** 배포 파일 목록과 일치하며 CRC 검사를 통과했습니다. 관리자 HTML은 제외했고, 기기에 설치된 APK의 SHA-256도 생성 파일과 일치했습니다.
- Galaxy Z Flip4에서 전역 애니메이션 설정 세 값을 모두 **0으로 유지**한 채 네이티브 검사 **4/4**를 통과했습니다. 실제 WebView의 시작·Activity 재생성 검사 2개와 제어된 시간의 Compose 오프닝 검사 2개입니다.
- 같은 후보 APK의 실제 WebView 화면 검사 **11/11**, 입력 검사 **12/12**를 통과했습니다. 홈에서 제거 대상 링크·QR이 없고 SQL 예제와 탐색이 남아 있으며, 키보드 표시로 화면 높이가 **800→476px**로 줄어도 빈 비밀번호란이 하단 메뉴 위에 보였습니다. 자격증명 입력이나 로그인·학습 제출은 수행하지 않았습니다.
- 오프닝 캡처 **8장**에서 닫힌 문, 열린 브랜드 배경, 이미 준비된 홈 위의 페이드와 안정된 홈을 직접 확인했습니다. 약 2.95초는 화면 구성을 시작한 뒤의 설정된 연출 시간으로 Android 프로세스 시작 시간은 제외하며, 캡처 검수를 정밀한 시간 측정으로 해석하지 않습니다.
- 홈의 합성 브라우저 검사 **129/129**도 통과했습니다. API·Socket·외부 요청을 대체하거나 차단한 검사로, 실제 운영 DB 저장이나 회원 기능 전체 검증을 의미하지 않습니다.

#### 운영 반영과 검증 범위

- 배포 전 전체 백업의 체크섬 **24항목**과 DB **49테이블**을 확인했습니다.
- 프런트엔드 소스 **3개 반영·2개 삭제**와 빌드 결과를 배포했습니다. 서버 재시작은 없었고 PM2의 프로세스 ID·누적 재시작 횟수·설정은 배포 전후 같았습니다.
- 정적 파일 **522개**와 실제 HTTPS로 받은 일반·관리자 HTML 및 직접 연결 CSS/JS **7개**의 SHA-256을 대조했습니다.
- 변경 대상 밖 운영 파일 **565개**, DB 스키마·보호 데이터·런타임 파일을 보존했습니다. 확인 구간의 서버 오류 로그 증가량은 **0바이트**였습니다.
- 운영 비회원 브라우저의 **6개 화면 조합, 화면 검사 48/48**과 실제 로딩 자산 **13개**의 SHA-256 일치를 확인했습니다. 운영 화면 캡처 **6장**도 직접 검수했습니다.
- 도구의 전체 집계는 **55/57**이며, 2항목은 의도된 소켓 차단의 분류 오류로 확인했습니다. 화면 **48/48**은 통과했으며 원본 결과와 별도 분석을 보존합니다. 이 운영 검사는 공개 GET/HEAD만 서버로 전송하고 방문 수 표시를 대체했으며 회원·쓰기·소켓·외부 요청은 차단했습니다.

이번 결과는 위 기기·최종 APK·검사 범위에 한정합니다. 운영 회원의 가입·승인·메일·학습 저장·동시 시험, 네이티브 전체화면과 모든 기기 호환성은 이번 확인 범위에 포함하지 않습니다. 아래 입력 보완 배포 기록의 수치와 APK는 당시 결과로 보존합니다.

### 모바일 키보드 입력란 가림 보완과 배포·APK 확인

실제 Android 앱에서 로그인 비밀번호란을 터치하면 키보드와 고정 하단 메뉴 때문에 입력란 일부가 가려지는 문제를 보완했습니다. 모바일 스크롤 여백을 조정해 입력란을 메뉴 위로 이동할 수 있게 했으며, 하단 메뉴는 계속 표시합니다. GitHub 반영, 운영 배포와 새 APK의 실기기 확인을 완료했습니다.

#### 운영 반영과 기존 데이터 보존

- 새 전체 백업을 만들고 백업 파일 **24개**의 체크섬과 DB **49개 테이블**을 검증했습니다.
- 키보드 보완은 CSS 소스 **1개**와 빌드 결과에 한정해 배포했습니다. 삭제 **0개**, 서버 재시작 **0회**이며 PM2 프로세스와 누적 재시작 횟수는 배포 전후 같았습니다.
- 최종 정적 파일 **496개**의 SHA-256을 확인했습니다. 실제 HTTPS로 받은 일반·관리자 HTML과 직접 연결된 CSS·JS **7개**도 배포 파일과 일치했습니다. 배포 확인 구간의 서버 오류 로그 증가량은 **0바이트**였습니다.
- DB 스키마, 보호 대상으로 대조한 개인·학습·콘텐츠·화면 설정 데이터와 운영 파일을 보존했습니다. CSS 이외 운영 소스 **569개**의 해시도 그대로 유지됐습니다.

#### 새 APK와 실제 기기 입력 확인

- APK 번들 `mobile-ui-20260907T145852425Z`의 웹 파일 **392개**가 배포 패키지와 일치하고 CRC 검사를 통과했습니다. 기기에 설치된 APK의 SHA-256도 제작 파일과 일치했습니다.
- Galaxy Z Flip4 **SM-F721N**에 기존 앱 데이터를 유지하는 방식으로 업데이트했습니다. 새 번들의 화면 검사 **11/11**, 입력 검사 **12/12**, 합계 **23/23** 항목이 통과했습니다.
- 실제 화면 **360×800px**에서 빈 비밀번호란을 터치했습니다. 키보드가 표시되어 WebView 높이가 **800→476px**로 줄어든 상태에서도 비밀번호란 전체가 하단 메뉴 위에 보이고 가로 넘침이 없었습니다. 비밀번호 입력 형식과 초점 유지, 뒤로 이동 후 홈 복귀도 확인했습니다.
- 자격증명은 입력하지 않았으며 로그인이나 학습 제출은 수행하지 않았습니다. APK 파일 일치와 위 기기 검사는 모든 Android 기기의 호환성을 의미하지 않습니다.

#### 검증 범위

| 검사 | 결과 | 확인한 범위 |
|---|---:|---|
| 백엔드 단위 검사 | **97/97** | 인증, 개인 학습 결과 저장, 설정과 기존 동작 |
| 프런트엔드 단위 검사 | **17/17** | 결과·오답 저장 처리, 설정·방문 통계 |
| 기능 합성 브라우저 | **119/119** | 360·411·1440px 화면, 관리자·필기·실기, 채점·오답 저장 요청과 저장 실패 안내 |
| 키보드 보완 합성 브라우저 | **51/51** | 라이트·다크 모드, 모바일 스크롤 여백과 데스크톱 영향 |
| 운영 비회원 브라우저 | **123/123** | **키보드 보완 전** 공개 화면 9개, 예제·QR, 화면 캡처 11장과 실제 자산 14개 일치 |
| 새 APK 실기기 화면 | **11/11** | 최종 번들 로드, 공개 홈·SQL 예제와 화면 상태 |
| 새 APK 실기기 입력 | **12/12** | 빈 비밀번호란 터치, 키보드 축소, 입력란 가시성과 홈 복귀 |

백엔드 구문, 변경 JS·JSX의 ESLint, CSS 파싱, 프런트엔드 빌드와 Git diff 공백 검사도 통과했습니다. 합성 검사는 대체 API 응답을 사용하는 검사 항목 수이며, 실제 사용자·기기 수나 운영 DB 저장 성공 건수가 아닙니다. 운영 비회원 **123/123** 결과는 이번 키보드 보완의 실기기 검증과 구분합니다.

채점 표시와 서버 저장 성공은 구분해 확인했습니다. 실제 회원의 가입·승인·메일·학습 결과 및 오답의 DB 저장을 앱에서 끝까지 수행하는 검증, 여러 사용자의 동시 시험, Android 회전·네이티브 전체화면과 모든 기기 호환성은 이번 확인 범위에 포함하지 않았습니다.

## 2026-09-07

### 전체 웹·모바일 UI 개편과 현행 소스 기준선 정리

9월 7일 UI 개편은 1차 GitHub 반영과 Lightsail 배포에 이어 보완 두 파일의 운영 반영, 당시 APK 설치와 실기기 화면 확인까지 완료했습니다. 아래 수치와 번들은 이 UI 개편 당시의 기록입니다. 1차 로컬·운영 검사, 1차 APK의 키보드 측정, UI 보완 검사와 당시 새 APK의 파일·화면 확인을 구분합니다. 이후 키보드 보완과 최신 APK 결과는 위 9월 8일 기록을 참조합니다.

#### 사용자 화면

- 홈을 학습 안내, 직접 조작하는 SQL 예제, 문제 풀이·해설 확인·오답 복습, 이용 안내와 관련 링크의 흐름으로 재구성했습니다. 홈페이지 예제는 설명용이며 개인 학습 기록에 저장되지 않는다고 표시합니다.
- 홈의 학습 시작은 비회원에게 로그인, 회원에게 학습 선택을 안내합니다. 비회원 보조 링크는 이용 방법으로 연결합니다.
- 필기·실기 로비뿐 아니라 문제은행, 기출 시험, 실기 3주 공략, 오답 복습, 멀티플레이의 대기·응시·결과 화면까지 문제·선지·답안·해설과 다음 행동이 읽히도록 정리했습니다.
- 마이페이지·기록·게시판·학습노트·로그인·가입·계정 찾기·FAQ·법적 동의·운세의 제목, 입력 순서, 빈 상태와 오류 안내를 조정했습니다.
- 기존 문서의 기능 설명을 현행 코드와 대조해 게시판 첨부·임시저장, 문제 오류신고, 학습노트 복구, 모바일 QR, 방문 통계와 관리자 개인정보 마스킹 안내를 README에 보완했습니다. 입장코드·CAPTCHA를 다시 요구하는 설명은 복원하지 않았습니다.
- 공통 색상·글자·간격·버튼·초점 표시를 맞추고, 모바일 탐색과 좁은 화면의 표·코드·이미지 표시를 정리했습니다. 라이트·다크 모드와 밝기 선택, 관리자에 저장된 화면 문구·링크·배너 설정은 유지합니다.
- 관리자 로그인과 관리 화면을 별도 운영 화면으로 정리하고, PC 탐색·모바일 선택, 목록·필터·상태·대화상자의 읽기 순서를 개선했습니다. 관리자 쿠키·CSRF·권한·사유 입력 절차는 유지합니다.

#### 게시판 이동과 기록 표시

- 자유게시판 상세 주소에 게시판 유형이 없을 때 기본 공지 유형과 실제 글 유형이 번갈아 적용되며 반복 조회되던 상태 갱신을 수정했습니다.
- 게시글 수정 진입 시 편집 대상 ID를 브라우저 이동 상태에 남겨, 뒤로·앞으로 이동할 때 수정 화면이 새 글 작성 상태로 잘못 복원되는 흐름을 보정했습니다. 편집 가능한 작성자·관리자 조건을 다시 확인합니다.
- 채점 결과 표시와 서버에 기록이 저장되었다는 사실은 구분합니다.

#### 접속·가입 정책과 배포 범위

- 검증된 운영 소스를 기준으로 저장소의 백엔드·프런트엔드를 동기화했습니다. 별도 관리자 앱, 회원·관리자 인증, 법적 동의, 방문 통계와 개인 기록 인증 등 이미 운영에 있던 변경을 새 UI 변경과 구분합니다.
- 현행 공개 접속에는 입장코드·hCaptcha를 요구하지 않습니다. 개인 기록·쓰기·멀티플레이 등 회원 기능의 세션 검증과 관리자 인증은 유지합니다.
- 가입 API는 이메일 인증과 약관 동의 후 관리자 승인 대기로 접수합니다. 자동가입·자동승인 전환 설정은 현재 소스에 없으며, 공개 접속 허용을 가입 자동승인으로 설명하지 않습니다.
- 맛집 지도·캘린더 일정은 현행 라우트에서 제외되어 README를 갱신했습니다. 아래 과거 변경 이력은 당시 기록으로 보존했습니다.
- 신규 UI의 운영 적용은 프런트엔드 소스와 빌드 결과에 한정합니다. 기준선에 포함된 백엔드 소스를 다시 배포하거나 DB·계정·문제 데이터·메일 설정을 바꾸는 작업은 이번 UI 배포 범위에 포함하지 않습니다.
- 1차 운영 반영은 전체 백업과 검증, 대상 파일·기존 상태 확인 후 진행했습니다. 저장소 동기화, 로컬 빌드, 운영 반영, APK 파일 검증과 실제 기기 기능 검증을 각각 구분합니다.

#### 1차 운영 반영과 실기기·APK 확인

- 사전 전체 백업을 완료하고 백업 파일 24개의 체크섬과 DB 49개 테이블을 검증했습니다.
- 프런트엔드 소스 68개와 빌드 결과를 배포했습니다. 삭제 파일 0개, 서버 재시작 0회이며 배포 확인 구간의 서버 로그에 추가 오류가 없었습니다.
- 운영 비회원 검사는 공개 화면·뷰포트 17개에서 **190/190** 통과했습니다. 실제 로드한 배포 자산 18개의 SHA-256이 해당 1차 빌드와 일치했고, 정상 비회원 401 응답 4건을 오류와 분리했습니다. 모든 쓰기·회원 조회·Socket·외부 연결을 차단한 범위입니다.
- 1차 APK 버전의 실제 Android 검사에서 운영 홈의 **360×800** 화면, 비회원 접속, 가로 넘침과 입장 CAPTCHA 부재를 확인했습니다. 키보드 표시 후 높이가 약 **476px**로 줄어들어도 입력창이 보이는 검사를 통과했습니다. 자격증명 입력과 로그인 제출은 수행하지 않았습니다.
- 1차 APK 패키지의 웹 자산 **395개**가 운영 파일과 일치하고 CRC 검사를 통과했습니다. 관리자 HTML은 APK에 포함하지 않은 것으로 확인했습니다. 이는 모든 Android 기기의 설치·실행·회원 기능 호환성을 확인했다는 뜻은 아닙니다.

#### 9월 7일 UI 보완 배포와 당시 APK 확인

- 빈 홈 보조 문구의 장식점과 터치 후 남는 hover 강조를 두 소스 파일에서 보정했습니다. 보완 합성 브라우저 검사 **19/19**를 통과한 뒤 새 빌드를 반영했습니다.
- 새 전체 백업의 체크섬 **24파일**과 DB **49테이블**을 검증하고 두 소스 파일의 추가 운영 배포를 완료했습니다. 배포된 정적 파일 **446개**의 SHA-256을 검증했습니다.
- PM2 프로세스 ID와 누적 재시작 횟수는 배포 전후 그대로 유지됐으며, 배포 확인 구간의 추가 서버 오류는 0건입니다. 공개·서버 내부의 일반 화면과 관리자 진입 화면은 200, 보호된 관리자 API는 비회원 401로 확인했습니다.
- 당시 UI 보완 APK 번들 `mobile-ui-20260907T131549488Z`에 포함된 웹 자산 **395개**가 운영 파일과 일치하고 CRC 검사를 통과했습니다. 관리자 HTML은 APK에서 제외했습니다.
- 실제 Galaxy Z Flip4에 해당 APK를 설치·실행해 **360×800** 화면을 확인했습니다. 가로 넘침과 빈 장식점이 없고 비활성 하단 메뉴 3개의 배경이 투명한 것을 확인했으며, 기기 화면 캡처도 직접 검수했습니다. **이 단락의 476px 키보드 측정은 위 1차 APK 버전의 결과**이며 해당 UI 보완 버전에서 재측정한 결과로 기록하지 않습니다.

#### 검증한 범위와 남은 확인

- 1차 UI의 백엔드 단위 검사 **86/86**, 프런트엔드 단위 검사 **17/17**, 합성 브라우저 검사 **742/742**가 통과했습니다. 백엔드 문법 검사, 전체 ESLint, 프런트엔드 빌드와 Git diff 공백 검사도 통과했습니다. 브라우저 검사 수는 실제 사용자·기기 수가 아닌 합성 환경의 검사 항목 수입니다.
- 로컬의 실제 React 화면에 합성 데이터를 연결하고 API·Socket.IO·외부 요청을 가로채 PC·모바일 표시와 기능 상태를 검사했습니다. 학습 화면은 320·360·375·411·1024·1440px 및 640·768px 전후 경계에서 확인했습니다.
- 선지·주관식 입력, 해설, 오답 재풀이, 저장 실패 표시, 시험 OMR·결과표, 멀티플레이 화면, 게시판 상세 반복 조회와 편집 이동 복원, 관리자 목록·승인 화면·대화상자 등 구현한 흐름을 합성 응답으로 점검했습니다.
- 이 검사는 운영 회원의 실제 가입·승인·메일, DB 저장, 여러 기기의 실제 Socket.IO 시험을 끝까지 검증한 결과가 아닙니다. 학습 테스트의 네이티브 전체화면 API는 대체했으며 실제 전체화면 진입·복귀 검증도 포함하지 않습니다.
- 1차 APK의 실기기 화면·키보드 표시 후 입력창 가시성, 각 APK의 파일 일치와 당시 UI 보완 APK의 실기기 설치·홈 화면은 위 범위에서 확인했습니다. Android 회전·네이티브 전체화면, 운영 회원의 가입·승인·메일·저장·동시 시험과 모든 기기의 APK 설치 호환성은 검증하지 않았습니다. 새 UI의 학습 성과·전환율·접근성 표준 전체 준수도 아직 입증하지 않았습니다.

#### 설계 참고

- [원본 영상](https://youtu.be/zJzdTyDxxi8?si=EMNykQScIZIldara): 사용자 상황에서 출발해 근거·이용 과정·다음 행동으로 이어지는 설명 순서를 참고했습니다.
- [ima2-gen 원본 저장소](https://github.com/lidge-jun/ima2-gen): 화면 목적별 정보 밀도, 디자인 기준과 반응형·상태 검토 지침을 참고했습니다.
- [DESIGN.md](./DESIGN.md): 우공실에 적용한 색상·레이아웃·입력·기능 보존 기준입니다. 영상의 성과 주장이나 생성 컨셉을 실제 서비스 성과·데이터로 표시하지 않습니다.

## 2026-06-26

### FIX22 최종 GitHub 반영

FIX22 이후 로컬과 운영 환경에 순차 반영했던 변경 사항을 GitHub `main` 브랜치에 기능별 커밋으로 정리해 반영했습니다. 이 작업은 제3자가 변경 범위를 추적할 수 있도록 문서, 백엔드 구조, 게시판, 학습노트, 실기 3주 공략, 오답노트, 관리자, 전역 UI를 서로 다른 성격의 커밋으로 분리했습니다.

#### 반영 커밋 범위

- `2026-06-26|로컬 생성 파일 제외 규칙 보강`
  - 로컬 DB 덤프, 환경 변수, 로그, 빌드 결과물, 업로드 파일, 실기/필기 이미지 자산 폴더가 GitHub에 올라가지 않도록 제외 규칙을 보강했습니다.
- `2026-06-26|공개 문서와 변경 이력 정리`
  - 공개용 README와 PATCH_NOTES를 제3자가 이해하기 쉬운 표현으로 정리했습니다.
- `2026-06-26|백엔드 라우트와 서비스 모듈 분리`
  - 관리자, 게시판, 학습노트, 실기, 멀티플레이, 사이트 관리 라우트와 공통 서비스를 기능 단위로 분리했습니다.
- `2026-06-26|게시판 BlockNote 편집기 전환`
  - 공지게시판과 자유게시판 구분은 유지하면서 글 작성/수정 화면을 BlockNote 기반 편집기로 전환했습니다.
- `2026-06-26|학습노트와 오답노트 문서화 기능 추가`
  - `/study` 학습노트, 폴더/문서 트리, 공개 범위 선택, `/오답노트` 삽입 흐름, 이미지 포함 오답의 BlockNote 이미지 블록 변환을 추가했습니다.
- `2026-06-26|실기 3주 공략과 답안 입력 개선`
  - 실기 3주 공략, 특수기호 입력 패드, 정답 판정 보정, 3주 공략 오류신고 연결을 반영했습니다.
- `2026-06-26|오답노트 출처 표시와 기출 화면 유틸 정리`
  - 오답노트와 학습노트에 삽입되는 문제의 출처를 필기 기출, 필기 문제은행, 실기 문제은행, 실기 기출, 실기 3주 공략 유형별로 표시하도록 보강했습니다.
- `2026-06-26|홈 화면과 전역 반응형 스타일 정리`
  - 홈 화면, 전역 테마 토큰, 반응형 스타일, 주요 페이지 버튼/텍스트 겹침 방지를 정리했습니다.

#### 오답노트와 학습노트 출처 표시

- 필기 기출 오답은 연도, 회차, 과목, 문제번호를 함께 표시합니다.
- 필기 문제은행 오답은 과목과 과목 내부 문제번호를 함께 표시합니다.
- 실기 문제은행 오답은 과목 또는 섹션 정보와 문제번호를 함께 표시합니다.
- 실기 3주 공략 오답은 주차, Section, `Section-문제번호` 형식을 함께 표시합니다.
- 이미지가 포함된 오답은 본문에 URL 문자열만 남기지 않고 BlockNote 이미지 블록으로 삽입합니다.

#### 학습노트 트리 사용성 보정

- 좌측 학습노트 트리는 루트, 폴더, 문서를 계층적으로 보여줍니다.
- 폴더 선택과 폴더 접기/펼치기 동작을 분리해 트리 구조 탐색이 명확하게 동작하도록 했습니다.
- 폴더 생성 버튼은 텍스트가 없는 작은 빈 버튼 대신 아이콘 버튼으로 표시합니다.

#### 검증

- `git diff --check` 통과
- 백엔드 `npm run check` 통과
- 프론트엔드 `npm run build` 통과
- GitHub 원격 `main` 브랜치가 최종 커밋을 가리키는지 확인
- 커밋 대상에서 제외해야 하는 파일과 폴더가 ignored 상태인지 확인

## 2026-06-25 ~ 2026-06-26

### FIX22 후속: 게시판 BlockNote, 학습노트, 운영 안정화

#### 코드 구조 정리

- 길어진 백엔드 라우트와 프론트엔드 화면 파일을 기능 단위로 분리했습니다.
- 관리자, 멀티플레이, 사이트 설정 라우트는 `backend/routes/*` 하위 모듈로 나눴습니다.
- 게시판 본문 처리, 게시판 업로드, 학습노트 스키마, 오답노트 스키마, 실기 기능 마운트 로직은 `backend/services/*` 영역으로 정리했습니다.
- 게시판 작성 화면, BlockNote 편집기, 임시저장 모달, 게시글 렌더링, 학습노트 화면은 프론트엔드 기능 파일로 분리했습니다.
- 이 정리는 내부 유지보수 목적이며, 기존 사용자 데이터와 주요 API 동작을 유지하는 범위에서 진행했습니다.

#### 게시판 편집기

- 공지게시판과 자유게시판 구분을 유지하면서 글 작성/수정 화면을 BlockNote 기반 편집기로 전환했습니다.
- 기존 게시글, 댓글, 좋아요, 조회수 저장 흐름은 기존 서비스 DB와 API를 그대로 사용합니다.
- 기존 텍스트 게시글은 계속 열람할 수 있으며, 신규 글은 구조화된 본문 데이터를 함께 저장할 수 있습니다.
- 기존 1000자 제한을 제거해 긴 학습 정리 글과 이미지가 포함된 게시글을 작성할 수 있도록 했습니다.
- 편집기 기본 배경을 흰색 계열로 조정하고, 한글 입력과 저장 과정에서 인코딩이 깨지지 않도록 확인했습니다.
- 게시판 파일 업로드는 `/api/posts/upload-file` API와 게시판 업로드 서비스로 연결했습니다.

#### 게시판 임시저장

- 글 작성 중 수동 임시저장, 브라우저 종료, 뒤로가기, 작성 취소 시 마지막 작성 내용을 임시저장하도록 보강했습니다.
- 임시저장 불러오기는 사용자가 `불러오기` 버튼을 직접 누를 때만 동작하도록 변경했습니다.
- 기존처럼 자동으로 임시저장 불러오기 팝업이 뜨는 흐름은 제거했습니다.
- 게시글 등록 또는 수정이 성공하면 제출 완료된 초안이 다시 임시저장으로 남지 않도록 정리했습니다.

#### 학습노트

- `/study` 학습노트 화면을 추가했습니다.
- 학습노트는 개인 비공개 문서를 기본값으로 하며, 필요 시 사용자가 공개 범위를 선택할 수 있습니다.
- 폴더와 문서 트리 구조를 추가해 오답 정리, 시험 내용 요약, 개인 복습 문서를 분리 관리할 수 있도록 했습니다.
- 좌측 학습노트 트리는 루트, 폴더, 문서를 계층적으로 표시하며, 화살표 버튼으로 하위 폴더와 문서를 접거나 펼칠 수 있습니다.
- 폴더 생성 버튼은 아이콘 버튼으로 명확하게 표시하고, 폴더 선택과 트리 접기/펼치기 동작을 분리했습니다.
- `wgs_study_folders`, `wgs_study_documents`, `wgs_study_document_wrong_refs` 테이블로 폴더, 문서, 오답 참조 관계를 저장합니다.
- `/오답노트` 명령과 오답노트 삽입 버튼을 통해 필기, 실기, 멀티플레이 오답을 문서에 삽입할 수 있도록 했습니다.
- 이미지가 포함된 오답은 URL 텍스트가 아니라 BlockNote 이미지 블록으로 삽입되도록 수정했습니다.
- 학습노트 파일 업로드는 `/api/study/upload-file` API로 분리했습니다.

#### 테마와 모바일 화면

- 학습노트 다크 모드와 라이트 모드에서 입력창, 카드, 모달, 버튼, 오답 카드의 글자색과 배경색이 테마에 맞게 보이도록 정리했습니다.
- 모바일 화면에서 버튼이 겹치거나 좁게 눌리는 문제를 줄이기 위해 버튼 그리드와 최소 폭을 조정했습니다.
- PC 화면에서는 넓은 레이아웃을 유지하고, 모바일에서는 주요 버튼과 입력 요소가 줄바꿈되도록 보정했습니다.

#### 운영 오류 대응

- 관리자 사용자 목록과 서비스 DB 상태를 점검해 사용자 데이터가 삭제된 문제가 아니라 API 경로와 로그 확인이 필요한 상황임을 검증했습니다.
- 서비스 DB의 사용자, 게시글, 실기 문제, 3주 공략 테이블 건수를 확인해 데이터 유지 상태를 확인했습니다.

#### 배포 검증

- 배포 전 데이터 보존 절차를 수행했습니다.
- 로컬과 배포 환경에서 `backend npm run check`를 통과했습니다.
- 로컬과 배포 환경에서 `frontend npm run build`를 통과했습니다.
- 게시판 임시저장 유틸 검증 통과
- 학습노트 오답 이미지 변환 검증 통과
- 운영 `/`, `/study`, `/board`, `/api/ipep/health` 정상 응답 확인
- 서비스 프로세스 상태와 주요 오류 로그를 확인했습니다.

## 2026-06-25

### FIX22 실기 3주 공략, 특수기호 입력, 실기 UI 정리

- 실기 로비에 `3주 공략` 탭을 추가했습니다.
- 사진 기반 3주 공략 문제를 CSV로 정리하고 운영 DB에 저장했습니다.
- CSV 기준 80문제, 20개 Section, 보기 이미지 18개를 문제번호/정답/해설 기준으로 교차검증했습니다.
- 사용자는 3주 공략에서 주차, Section, 섹션순/랜덤 정렬을 선택해 문제를 풀 수 있습니다.
- 3주 공략 문제도 서버 채점 API를 사용하며, 문제 풀이 중에는 정답 원문이 노출되지 않도록 했습니다.
- 실기 정답 판정에서 영어 대소문자, 앞뒤 공백, 쉼표/줄바꿈 구분, 일부 문장부호 차이를 더 유연하게 처리했습니다.
- `①`, `②`, `③`처럼 모바일에서 입력하기 어려운 원 안 숫자 정답은 일반 숫자와 쉼표 구분 입력도 정답으로 인정하도록 했습니다.
- 실기 답안 입력창 위에 `특수기호 열기/닫기` 토글을 추가했습니다.
- 특수기호 패드는 설명 없이 기호만 제공하고, 클릭하면 현재 답안 입력창에 바로 삽입됩니다.
- `∪`, `∩`, `×`, `π`, `⋈`, `<< >>`, `0..*`, `%d`, `\n` 등 실기/필기 풀이에 자주 필요한 기호를 중복 없이 정리했습니다.
- 실기 문제은행과 실기 3주 공략 화면을 필기 문제은행처럼 간결한 필터, 접힘 가이드, 접힘 문제 이동 영역 중심으로 정리했습니다.
- 3주 공략 각 문제에 오류신고 기능을 추가했습니다.
- 배포 검증:
  - CSV 구조와 보기 이미지 경로 검증 통과
  - 3주 공략 정답/해설 교차검증 통과
  - `backend npm run check` 통과
  - `frontend npm run build` 통과
  - 실기 문제은행, 실기 3주 공략, IPEP health 경로 정상 응답 확인
  - 모바일/PC에서 실기 문제은행과 3주 공략 가로 overflow 없음 확인
  - 3주 공략 오류신고 모달 정상 표시 확인

## 2026-06-22

### FIX21 QR/이미지 운영 안정화

- 홈 QR 코드는 `/api/mobile-qr` 프록시를 통해 PNG로 응답하도록 운영에서 검증했습니다.
- 배포 전 데이터 보존 절차와 기본 응답 검증을 수행했습니다.
- 운영 검증 중 누락된 `backend/public/ipep-img` 실기 이미지 자산을 반영해 `/ipep-img/...` 경로가 실제 `image/png`로 응답하도록 보정했습니다.
- 운영 검증 결과 `/`, `/login`, `/mypage`, `/api/ipep/health`, `/question_image/...`, `/ipep-img/...`, `/api/mobile-qr?...` 응답이 정상임을 확인했습니다.

### FIX21 추가: 관리자 접속자 집계 구분

- 관리자 계정이 홈 화면의 접속자 목록과 현재 인원 수에 포함되지 않도록 제외했습니다.
- 배포 검증:
  - `node --check` 통과
  - `npm run build` 통과
  - 관리자 계정 접속자 제외 단위 검증 통과
  - 서비스 프로세스 상태 확인
  - 홈, IPEP health, 모바일 QR, 필기/실기 이미지 경로 `200` 응답 확인

## 2026-06-20

### FIX20 보안, 접속 보호, 관리자 운영 안정화

#### 접속 보안

- 첫 접속 기기에서는 게이트키퍼 인증을 거치도록 유지했습니다.
- 게이트키퍼 인증을 통과한 기기에서는 로그인, 회원가입, 계정 찾기 등에서 hCaptcha 반복 입력을 줄였습니다.
- 캐시 삭제, 기기 변경, 브라우저 변경, 인증 쿠키 만료 시에는 다시 보안 확인이 필요할 수 있습니다.
- 모바일 로컬 접속 환경에서 게이트키퍼 인증 쿠키가 저장되지 않던 문제를 보정했습니다.

#### API 보호

- 사용자의 주요 데이터 변경 API가 클라이언트에서 보낸 `userId`만 믿지 않도록 개선했습니다.
- 오답노트, 학습 결과, 운세 기록, 게시판, 댓글, 멀티플레이, 관리자 API는 서버 세션과 권한을 다시 확인합니다.
- 다른 사용자의 ID를 개발자도구에서 임의로 넣어 조회하거나 수정하려는 요청은 차단됩니다.
- 주요 쓰기 API와 조회 증가 API에 요청 제한을 추가했습니다.

#### 자동화와 크롤러 방어

- 보안 헤더를 추가해 iframe 삽입, MIME sniffing, 과도한 referrer 노출을 줄였습니다.
- Content Security Policy를 적용해 허용된 스크립트, 이미지, hCaptcha 리소스 범위를 명확히 했습니다.
- `robots.txt`와 `X-Robots-Tag`를 적용해 검색 엔진과 자동 크롤러의 색인화를 제한했습니다.
- CORS 허용 범위를 제한해 브라우저 기반 외부 origin 요청을 줄였습니다.

#### 의존성 보안

- backend와 frontend의 npm audit 취약점을 0개로 정리했습니다.
- `nodemailer`, `axios`, `react-router-dom`, `vite`, `postcss` 등 직접 의존성을 안전 버전으로 갱신했습니다.
- `ws`, `qs`, `form-data`, `follow-redirects`, `js-yaml`, `@babel/core` 등 하위 의존성은 취약 범위 밖 버전으로 고정했습니다.

#### 성능과 번들

- 프론트엔드 라우트를 지연 로딩해 초기 메인 번들 크기를 줄였습니다.
- 기존 1000kB 초과 번들 경고가 사라졌고, 주요 페이지는 필요한 시점에 나뉘어 로드됩니다.

#### 관리자 화면

- 회원 목록의 최근 로그인/최근 로그아웃 의미를 확인했습니다.
- 최근 로그인은 로그인 성공 시각이고, 최근 로그아웃은 명시적 로그아웃 API 호출 시각입니다.
- 브라우저 종료, 서버 재시작, 탭 종료는 명시적 로그아웃으로 기록되지 않을 수 있습니다.
- `로그인 유지`와 `현재 접속중`은 다른 개념이므로, 이후 화면에서 더 명확히 표시할 개선 후보로 정리했습니다.

#### 운영 검증

- 배포 전 애플리케이션 파일과 DB 보존 절차를 수행했습니다.
- 백엔드 점검과 프론트엔드 빌드를 통해 기본 배포 가능 상태를 확인했습니다.
- 운영 반영 후 `/`, `/robots.txt`, `/api/ipep/health`가 정상 응답하는 것을 확인했습니다.

## 2026-06-19

### FIX19 화면 테마와 문제 랜덤 개선

#### 화면과 테마

- 다크 모드와 라이트 모드에서 배경, 글자, 카드, 버튼 색상이 자연스럽게 보이도록 정리했습니다.
- 화면 밝기를 10% 단위로 조절할 수 있도록 개선했습니다.
- 밝기 조절이 일부 영역에만 어색하게 적용되는 문제를 줄이고, 전체 화면 톤을 일관되게 보정했습니다.
- 홈 화면의 메인 문구와 주요 안내 문구 위치가 안정적으로 보이도록 정리했습니다.
- 로그인, 회원가입, 마이페이지, FAQ, 게시판, 운세, 문제 풀이 화면의 안내 문구와 버튼명이 더 읽기 쉽도록 정리했습니다.

#### 문제 풀이

- 필기와 실기 랜덤 문제에서 방금 풀었던 문제가 바로 다시 나올 가능성을 줄였습니다.
- 전체 과목 랜덤 풀이에서는 최근에 나온 과목이 과도하게 반복되지 않도록 개선했습니다.
- 문제 수가 부족한 경우에는 조건을 완화해 문제 풀이가 멈추지 않도록 처리했습니다.
- 실기 문제, 기출 문제, 오답노트의 안내 문구와 결과 문구를 더 명확하게 정리했습니다.

#### 회원가입 승인

- 신규 회원가입은 바로 가입 완료되지 않고 관리자 승인 대기 상태로 접수되도록 변경했습니다.
- 관리자가 승인하면 실제 계정이 생성되고, 승인 안내 메일이 발송됩니다.
- 관리자가 거절하면 거절 사유가 포함된 안내 메일이 발송됩니다.

## 2026-05-28

### 입장 인증과 보안 강화

- 사이트 진입 전 초대 코드 기반 게이트키퍼 인증을 적용했습니다.
- 게이트키퍼, 로그인, 회원가입, 인증 메일 발송, 계정 찾기, 비밀번호 변경 구간에 hCaptcha 보안 확인을 적용할 수 있도록 구성했습니다.
- 클라이언트, 이메일, IP 기준 요청 제한을 분리해 반복 요청과 자동화 시도를 줄였습니다.
- 인증되지 않은 API 요청을 차단하는 보호 흐름을 추가했습니다.

## 2026-05-27

### 운영 공지와 점검 모드

- 관리자가 전체 사용자에게 운영 공지를 발송할 수 있는 기능을 추가했습니다.
- 점검 모드를 켜면 일반 사용자는 점검 안내 화면으로 전환되고, 관리자 권한 사용자는 계속 접근할 수 있도록 구성했습니다.
- 점검 상태를 주기적으로 확인해 사용자가 새로고침하지 않아도 점검 화면으로 전환되도록 개선했습니다.
- 전체 공지와 점검 모드 변경 이력을 운영 로그로 확인할 수 있도록 보강했습니다.

## 2026-05-24

### 관리자 결재와 권한 관리

- 운영자 권한 사용자가 요청한 민감 작업을 최고관리자가 승인 또는 반려하는 결재 흐름을 추가했습니다.
- 사용자 임시정지, 계정 삭제, 운영자 권한 변경 같은 작업은 권한에 따라 즉시 반영 또는 결재 대기 방식으로 처리됩니다.
- 결재 요청의 상태, 요청자, 요청 내용, 처리 결과를 관리자 페이지에서 확인할 수 있도록 보강했습니다.
- 관리자 작업 이력을 DB에 저장해 운영 변경 이력을 추적할 수 있도록 구성했습니다.

## 2026-05-21

### 운영 관리 화면 확장

- 화면 문구, 버튼명, 이미지, 링크, 색상, 레이아웃 값을 관리자 페이지에서 관리할 수 있도록 화면 설정 기능을 추가했습니다.
- 문제, 해설, 표시 문구를 운영 DB와 연결된 상태에서 관리할 수 있도록 관리자 API 구조를 보강했습니다.
- 로그인 기록, 오답노트, 멀티플레이 결과 등 운영 데이터 조회 흐름을 정리했습니다.

## 2026-05-18

### 멀티플레이 CBT

- 필기 랜덤 CBT 멀티플레이 기능을 추가했습니다.
- 방 생성, 방 번호와 인증 비밀번호 기반 입장, 준비 완료, 시험 시작 흐름을 제공합니다.
- 같은 방에 입장한 사용자는 동일한 랜덤 CBT 문제를 풀 수 있습니다.
- 방 번호와 인증 비밀번호로 제출 기록과 채점 결과를 확인할 수 있습니다.
- 멀티플레이에서 틀린 문제를 다시 불러와 복습할 수 있는 오답 복습 기능을 추가했습니다.

## 2026-05-15

### 정식 운영 기반 배포

- 로컬 중심으로 운영되던 서비스를 클라우드 서버 기반으로 이전했습니다.
- 24시간 접근 가능한 배포 구조를 정리했습니다.
- 문제, 회원, 게시판, 오답노트 등 주요 데이터를 운영 데이터베이스로 이전하고 검증했습니다.
- 정보처리기사 실기 문제 화면과 과목/회차 기반 문제 데이터 구조를 운영 환경에서 사용할 수 있도록 보강했습니다.
- 사이트 입장 코드, 운영 주소, 메일 인증, 관리자 설정 등 배포 환경 설정을 정리했습니다.

## 2026-05-14

### 베타 업데이트

- PC, 태블릿, 모바일 환경에서 화면이 자연스럽게 보이도록 반응형 구조를 개선했습니다.
- 홈 화면과 주요 UI의 여백, 배치, 콘텐츠 표시 방식을 정리했습니다.
- 필기 문제은행과 기출 문제 해설 기능을 추가했습니다.
- 문제 풀이 결과 화면에서 정답, 오답, 해설을 함께 확인할 수 있도록 개선했습니다.
- 필기 기출 문제 결과를 PDF로 출력하고 오답노트에서 문제, 보기, 정답, 해설을 함께 확인할 수 있도록 개선했습니다.

## 2026-05-13

### 게시판 이용 안내

- 비로그인 사용자는 게시글 목록과 공지글을 확인할 수 있도록 안내했습니다.
- 일반 게시글 열람, 작성, 수정, 삭제, 댓글, 공감, 추천 기능은 로그인 후 이용하도록 정리했습니다.
- 본인이 작성한 글과 댓글을 확인하는 활동 이력 흐름을 안내했습니다.
- 댓글, 공감, 추천 활동에 대한 이메일 알림 발송 방식을 정리했습니다.
- 개인정보 노출, 광고, 부적절한 글에 대한 운영 기준을 공지했습니다.

## 2026-05-12

### 학습 화면과 사용 안내

- 중복 로그인, 세션 만료, 서버 업데이트 상황에서 사용자 안내 메시지가 표시되도록 개선했습니다.
- 홈 화면의 운영 안내 영역을 메인 배너 안으로 통합했습니다.
- 시험일까지 남은 D-Day와 주요 바로가기 버튼을 홈 화면에서 확인할 수 있도록 구성했습니다.
- 로그인 사용자는 현재 접속자 목록을 확인할 수 있습니다.

## 2026-05-10

### 다크 모드와 FAQ

- 다크 모드와 라이트 모드 전환 시 일부 글자가 흐리거나 보이지 않던 문제를 수정했습니다.
- 필기/실기 로비, 문제 풀이, 게시판, FAQ, 마이페이지, 오답노트 화면의 가독성을 보완했습니다.
- FAQ 화면을 정식 문서 형식으로 정리하고 검색과 페이지 이동 구조를 개선했습니다.
- 실기 문제 이미지 보조 자료를 확대하거나 축소할 수 있도록 개선했습니다.

## 2026-05-09

### 실기 문제 메뉴와 데이터 구조

- 정보처리기사 실기 문제 메뉴를 추가했습니다.
- 실기 랜덤 문제, 실기 기출 문제, 실기 결과 PDF 출력 기능을 추가했습니다.
- 필기와 실기 문제 로비 구조를 분리했습니다.
- JSON 기반 데이터 관리를 MySQL 기반 구조로 전환했습니다.
- 이메일 인증 환경 변수 문제를 수정했습니다.
- 모바일 접속 QR 기능을 개선했습니다.

## 배포 전 검증 기준

Backend:

```bash
cd backend
npm audit
npm run check
```

Frontend:

```bash
cd frontend
npm audit
npm run lint
npm run build
```

배포 후에는 `/`, `/cert/ipe`, `/login`, `/manage/`, `/api/ipep/health` 응답과 실제 학습·관리자 화면을 확인합니다. 오답·학습노트 저장과 재조회, 회원·관리자 권한 구분도 함께 검증합니다.
