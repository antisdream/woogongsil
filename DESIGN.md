# 우공실 디자인 기준

작성일: 2026-09-07. 정보처리기사 학습을 시작하고, 문제를 이해하고, 오답을 다시 공부하는 전체 웹·모바일 경험을 정돈한다. 기존 계정·관리자 인증, 문제·채점·기록 저장 계약은 유지한다.

## 근거와 적용

- [상세페이지 설계 영상](https://www.youtube.com/watch?v=zJzdTyDxxi8): 고객 상황 → 원하는 변화 → 실제 근거 → 사용 과정 → 다음 행동의 순서. 자동 자막 전체를 분석했으며, 영상 화면 검증이나 성과 수치 검증을 뜻하지 않는다.
- [ima2-gen](https://github.com/lidge-jun/ima2-gen/tree/36aa6fcea62f753d858d20c4823e04922951c954): `ima2-uiux`, `ima2-front` 가이드의 화면 목적별 밀도, 디자인 기준 선행, 반응형·상태·접근성 검토를 참고했다. Codex의 도구로 구현하며 원격 서비스 설치·로그인은 하지 않는다.
- 생성한 컨셉 보드는 구도·색·위계의 참고다. 모바일 선지 누락·임의 문항 수처럼 실제 기능과 다른 이미지 요소는 구현하지 않는다.

## 시각 체계

- 성인 학습자의 차분한 온라인 학습장. 기본 라이트: 배경 `#f6f8fc`, 종이 `#ffffff`, 본문 `#15233e`, 보조 `#52617a`, 구분선 `#dce3ee`, 주요 동작 `#3157d5`. 초록은 정답·성공, 빨강은 오류·삭제에만 사용한다.
- 기존 다크 모드와 밝기 조절을 보존한다. 밝기 50의 기본 토큰을 새 색에 맞추고 저장된 사용자 선택은 유지한다.
- 글꼴: 운영체제의 한국어 시스템 폰트. 폰트 파일을 새로 구매·배포하지 않는다. 본문 16px/1.65, 보조 14px, 제목 28~42px, 랜딩 제목 34~58px. 한국어 제목 keep-all/balance, 본문 overflow-wrap:anywhere.
- 모서리: 입력·버튼 10px, 패널 16px. 그림자는 떠 있는 메뉴·대표 패널에만 약하게 적용한다. 무지개 버튼·이모지 장식·과도한 그라데이션을 없애고 기존 react-icons의 SVG를 쓴다.
- 간격은 4/8/12/16/24/32/48/64px. 대화형 요소 최소 44px, 입력 폰트 16px, 초점 테두리와 비활성 상태를 보인다.

## 화면별 역할

| 화면 | 밀도·최우선 행동 | 구성 |
|---|---|---|
| 홈 | 여유 있게, 학습 시작 | 짧은 약속과 지원 시험 → 실제 조작 가능한 자체 예제 → 학습 3단계 → 실제 랭킹·커뮤니티 → FAQ·보조 링크 |
| 자격증·필기·실기 로비 | 보통, 모드 선택 | 현재 위치, 간결한 설명, 학습 방식별 목적·차이·시작 버튼 |
| 문제·시험·오답 | 집중, 답 제출·확인 | 소형 헤더, 문제·보기·답안 우선, 보조 도구 분리, 명확한 채점 결과와 다음 행동 |
| 기록·마이페이지 | 보통, 내 기록 찾기 | 실제 데이터, 의미 있는 빈 상태, 학습으로 연결, 개인정보 조작 독립 |
| 게시판·노트 | 보통, 읽기·작성 | 목록·본문 위계, 검색·분류, 편집기 읽기 폭, 글·댓글·초안·첨부 보존 |
| 계정·FAQ·법적 안내·운세 | 보통, 현재 단계 완료 | 입력·안내 순서, 현재 상태·오류·재시도·다음 행동, 동의 버전·범위 보존 |
| 관리자 | 조밀, 조회·승인·운영 | 좌측 탐색/모바일 선택, 정보 표와 필터, 위험 동작 분리, 쿠키·CSRF·이유 입력 보존 |

## 공통 구성 규칙

- `--wgs-*` 기존 토큰을 유지하고 주요 값만 맞춘다. 새 공통 클래스는 `ui-*`로 명명한다. `ui-page-head`, `ui-eyebrow`, `ui-page-description`, `ui-primary`, `ui-secondary`, `ui-panel`, `ui-empty`를 제공한다.
- 최상위 shell은 데스크톱 약 1280px 읽기 폭, 시험 시 약 1440px. 모바일 16px 여백(360px 이하 12px), 표·코드·그림 영역만 내부 스크롤을 허용한다.
- 320/360/375/411/768/1024/1440px 및 640/768 경계에서 확인한다. 고정 하단 탐색은 safe-area와 본문 여백을 함께 적용하며 화면 키보드·시험 조작을 가리지 않는다.
- 랜딩과 학습 페이지에 하나의 H1을 둔다. 브랜드는 링크이고 메뉴에는 현재 위치와 명확한 레이블이 있다. 메뉴·팝오버는 키보드/닫기/Escape를 지원한다. reduced-motion을 따른다.
- 관리자가 바꿀 수 있는 실제 제목·설명·링크·배너 설정은 계속 반영한다. 장식적인 기본값만 개선하며 관리 저장값을 덮어쓰지 않는다.

## 기능·콘텐츠 계약

- 자체 SQL 예제는 홈페이지 설명용이며 저장되지 않는다고 표시한다. 실제 문제 수·연속 학습일·합격률·후기·자동 저장 여부를 만들어 표시하지 않는다.
- 채점 결과와 저장 성공은 별개다. 기존 회원 ID/세션·관리자 쿠키/CSRF·법적 동의·중복 접속·시간 제한·시험 이탈·오답 모드·인쇄 기능을 보존한다.
- 비회원과 회원이 이용할 수 있는 범위를 실제 코드에 맞춰 안내한다. 로그인 필요 기능은 이유와 로그인 경로를 제공한다.
- 모든 화면의 로딩·빈 값·정상·권한 거부·오류·긴 내용 상태를 점검한다. 시각 QA는 합성 데이터와 차단된 외부 요청을 사용한다. 운영 데이터 쓰기·계정 생성·메일 발송은 QA에 포함하지 않는다.
- 새 UI의 효과는 아직 측정하지 않았다. 빌드·정적 검사·fixture 동작·운영 비회원 확인·실제 Android 화면 근거를 각각 구분해 기록한다.
