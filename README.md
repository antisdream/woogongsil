# 우공실

우공실은 정보처리기사 필기와 실기 학습을 위한 웹 기반 학습 서비스입니다. 문제 풀이, 기출 복습, 오답노트, 랭킹, 멀티플레이 CBT, 게시판, 맛집 지도, 관리자 운영 기능을 한 서비스 안에서 제공합니다.

운영 서비스: https://woogongsil.site

## 주요 기능

- 필기 문제은행, 기출 문제, 랜덤 문제 풀이
- 실기 랜덤 문제, 실기 기출 문제, 주관식 답안 입력
- 오답노트, 개인 풀이 기록, 랭킹 기록
- 멀티플레이 CBT 방 생성, 참여, 결과 확인
- 게시판, 공지, 댓글, 좋아요, 이메일 알림
- FAQ, 운세, 캘린더, 실시간 접속자와 채팅
- 맛집 지도, 장소 제보, 수정/삭제 요청, 관리자 검토
- 회원가입 승인 대기, 관리자 승인/거절, 안내 메일 발송
- 관리자 페이지 기반 사용자, 공지, 문제, 화면 문구, 일정, 맛집, 승인 요청 관리

## 보안과 접속 흐름

우공실은 제한된 학습 구성원이 사용하는 서비스를 기준으로 설계되어 있습니다.

- 첫 접속 기기에서는 게이트키퍼 인증을 통과해야 서비스를 사용할 수 있습니다.
- hCaptcha는 게이트키퍼, 로그인, 회원가입, 계정 찾기처럼 자동화 공격 위험이 있는 흐름에 적용됩니다.
- 게이트키퍼 인증을 통과한 신뢰 기기에서는 로그인 이후 반복적인 hCaptcha 입력을 줄였습니다.
- 캐시 삭제, 브라우저 변경, 기기 변경, 게이트키퍼 쿠키 만료 시에는 다시 보안 확인이 필요할 수 있습니다.
- 주요 API는 서버에서 세션 토큰과 사용자 권한을 다시 검증합니다.
- 개발자도구에서 화면 디자인이나 CSS를 임시로 바꿔도 서버 데이터는 바뀌지 않습니다.
- 서버 데이터를 변경하는 작업은 인증된 API와 권한 검사를 통과해야만 반영됩니다.
- 요청 제한, CORS 제한, 보안 헤더, robots 차단 정책을 적용했습니다.

## 회원가입 승인

신규 회원가입은 바로 계정으로 등록되지 않고 관리자 승인 대기 상태로 접수됩니다.

1. 사용자가 회원가입을 신청합니다.
2. 신청 정보는 승인 대기 목록에 저장됩니다.
3. 관리자가 승인하면 실제 계정이 생성됩니다.
4. 관리자가 거절하면 거절 사유와 함께 안내 메일이 발송됩니다.

이 방식은 승인되지 않은 사용자의 접근을 줄이고, 소규모 학습 그룹 운영에 맞춰 회원 관리를 명확하게 하기 위한 흐름입니다.

## 관리자 기능

관리자 페이지에서는 다음 기능을 제공합니다.

- 회원 목록과 권한 상태 확인
- 가입 승인 요청 처리
- 사용자 임시정지, 계정 삭제, 운영자 권한 관리
- 전체 공지 발송과 운영 모드 관리
- 게시판 공지 순서와 알림 관리
- 문제와 해설 조회 및 수정
- 화면 문구와 레이아웃 설정 관리
- 캘린더 일정 관리
- 맛집 장소와 수정 요청 검토
- 운영 로그와 최근 접속 기록 확인

회원 목록의 `최근 로그인`은 로그인 API가 성공한 시각입니다. `최근 로그아웃`은 사용자가 명시적으로 로그아웃 API를 호출한 마지막 시각입니다. 브라우저 종료, 서버 재시작, 탭 종료는 명시적 로그아웃으로 기록되지 않을 수 있으므로 `로그인 유지`와 `현재 접속중`은 구분해서 해석해야 합니다.

## 기술 스택

Frontend:

- React 19
- Vite 8
- React Router 7
- Axios
- Socket.IO Client
- Tailwind CSS
- ESLint

Backend:

- Node.js
- Express 5
- MySQL
- Socket.IO
- bcrypt
- Nodemailer
- csv-parser / csv-parse

Infrastructure:

- AWS Lightsail
- Ubuntu
- PM2
- Nginx
- MySQL

## 프로젝트 구조

```text
ExamAppProject/
├─ backend/
│  ├─ config/          # 환경 변수와 DB 연결 설정
│  ├─ middleware/      # 게이트키퍼, hCaptcha, 요청 제한
│  ├─ routes/          # 기능별 API 라우트
│  ├─ services/        # 공통 서버 상태와 저장소 유틸
│  ├─ scripts/         # 문제 데이터 적재와 유지보수 스크립트
│  └─ server.js        # Express/Socket.IO 서버 진입점
├─ frontend/
│  ├─ public/          # 정적 파일
│  └─ src/
│     ├─ components/   # 공통 UI 컴포넌트
│     ├─ features/     # 기능별 모듈
│     ├─ pages/        # 라우트 페이지
│     └─ styles/       # 전역/관리자/기능별 스타일
├─ PATCH_NOTES.md      # 사용자 중심 변경 이력
└─ README.md
```

## 로컬 실행

Backend:

```bash
cd backend
npm install
npm run check
node server.js
```

Frontend:

```bash
cd frontend
npm install
npm run lint
npm run build
```

개발 서버가 필요하면 frontend에서 다음 명령을 실행합니다.

```bash
npm run dev
```

기본 백엔드 health check:

```bash
curl http://localhost:5000/api/ipep/health
```

## 환경 변수

실제 운영 값은 저장소에 포함하지 않습니다. 예시는 `backend/.env.example`을 참고합니다.

주요 환경 변수 범위:

- 서버 포트
- MySQL 연결 정보
- 공개 사이트 URL
- 게이트키퍼 초대 코드와 토큰 secret
- hCaptcha site key와 secret key
- 메일 발송 계정
- Kakao 지도 API 키
- CORS 허용 origin
- 요청 제한 값

GitHub에 올리지 않는 항목:

- `.env`
- SSH 키
- 운영 DB 덤프
- 메일 앱 비밀번호
- hCaptcha secret
- 초대 코드
- Kakao API 키
- `node_modules`
- `frontend/dist`
- 백업 압축 파일

## 검증 기준

배포 또는 PR 전에는 다음 검증을 권장합니다.

```bash
cd backend
npm audit
npm run check
```

```bash
cd frontend
npm audit
npm run lint
npm run build
```

운영 반영 후에는 다음 항목을 확인합니다.

- PM2 프로세스 online
- Nginx 설정 검사 통과
- `/`, `/admin`, `/mealmap` HTTP 200
- `/api/ipep/health` 정상 응답
- 게이트키퍼 인증 흐름 정상
- 관리자 페이지 접근과 주요 목록 조회 정상
- 모바일 화면에서 표, 지도, 버튼, 입력창 표시 정상

## 문서

- [PATCH_NOTES.md](./PATCH_NOTES.md): 사용자 중심 변경 이력
- [backend/.env.example](./backend/.env.example): 백엔드 환경 변수 예시
- [docs/performance/README.md](./docs/performance/README.md): 성능 검증 관련 문서

## 라이선스와 공개 범위

이 저장소는 애플리케이션 소스 공개를 기준으로 정리됩니다. 운영 데이터, 계정 정보, 비밀 키, 백업 파일은 포함하지 않습니다.

## FIX21 업데이트 요약 - 2026-06-22

- 마이페이지에 개인 달력 일정 관리 기능을 추가했습니다.
- 관리자는 달력 일정 관리에서 전체 공통 일정과 사용자별 배정 일정을 구분해 관리할 수 있습니다.
- 사용자별 일정은 선택된 사용자에게만 보이며, 각 사용자는 본인 일정만 관리할 수 있습니다.
- 관리자/사용자 달력 일정 목록에 삭제 기능을 추가했습니다.
- 일정 입력 검증 메시지는 토스트 알림 중심으로 정리했습니다.
- 홈 QR 코드는 운영에서 `200 image/png` 응답을 확인했습니다.
- 배포 전 데이터 보존 절차와 기본 응답 검증을 수행했습니다.
- 누락된 실기 이미지 자산 `backend/public/ipep-img`를 반영해 이미지 경로 응답을 보정했습니다.

## FIX21 추가 업데이트 - 관리자 실시간 접속자/채팅 구분 (2026-06-22)

- 관리자 계정은 홈 화면의 실시간 접속자 현황과 접속자 수 집계에서 제외했습니다.
- 관리자가 실시간 채팅을 보낼 때 메시지에 관리자 역할 정보를 포함하고, 채팅창과 팝업창에서 관리자 메시지를 별도 색상과 `관리자` 배지로 표시하도록 정리했습니다.
- 배포 전 데이터 보존 절차를 수행하고, 배포 후 백엔드 문법 검사, 프론트엔드 빌드, 홈/헬스체크/QR/이미지 경로 응답을 확인했습니다.

## FIX22 업데이트 요약 - 2026-06-25

- 실기 로비에 `3주 공략` 탭을 추가했습니다.
- 3주 공략 문제 80개를 Section 기준 CSV로 정리하고 DB에 저장했습니다.
- 주차, Section, 섹션순/랜덤 정렬 필터로 3주 공략 문제를 풀 수 있습니다.
- 실기 답안 입력창에 특수기호 토글 패드를 추가해 모바일에서도 기호를 클릭 입력할 수 있습니다.
- 특수기호 패드는 시험 보조 설명 없이 기호만 제공하며, 중복 기호는 제거했습니다.
- 영어 대소문자, 앞뒤 공백, 쉼표/줄바꿈 구분, 원 안 숫자와 일반 숫자 차이처럼 입력 편의상 허용 가능한 정답 판정 범위를 보정했습니다.
- 실기 문제은행과 3주 공략 화면을 더 간결한 필터와 접힘 영역 중심으로 정리했습니다.
- 3주 공략 각 문제에 오류신고 기능을 추가했습니다.
- 배포 전 문제 데이터와 DB 변경 범위를 확인하고, 배포 후 백엔드 점검, 프론트엔드 빌드, 모바일/PC 화면 검증을 완료했습니다.

## FIX22 후속 업데이트 요약 - 2026-06-25~2026-06-26

- 유지보수성을 높이기 위해 길어진 백엔드 라우트, 서비스 로직, 프론트엔드 화면 파일을 기능 단위로 분리했습니다.
- 게시판 글 작성/수정 화면을 BlockNote 기반 편집기로 전환했습니다.
- 공지게시판과 자유게시판 구분, 게시글/댓글/좋아요/조회수 저장 흐름은 기존 DB와 API를 그대로 유지했습니다.
- 기존 1000자 제한을 제거해 긴 학습 정리 글과 이미지가 포함된 문서를 작성할 수 있도록 했습니다.
- 게시판 임시저장은 사용자가 직접 불러오기를 실행할 때만 표시되도록 변경하고, 브라우저 종료나 뒤로가기 상황에서는 마지막 작성 내용을 저장하도록 보강했습니다.
- `/study` 학습노트 화면을 추가했습니다.
- 학습노트는 개인 비공개를 기본값으로 하며, 사용자가 공개 범위를 선택할 수 있습니다.
- 폴더/문서 트리 구조와 `/오답노트` 삽입 흐름을 추가해 필기, 실기, 멀티플레이 오답을 개인 문서에 정리할 수 있도록 했습니다.
- 좌측 학습노트 트리는 루트, 폴더, 문서를 계층적으로 표시하며 접기/펼치기 토글과 폴더 생성 아이콘 버튼을 제공합니다.
- 이미지가 포함된 오답은 URL 텍스트가 아니라 BlockNote 이미지 블록으로 들어가도록 보정했습니다.
- 학습노트 다크 모드와 라이트 모드의 글자색, 배경색, 입력창, 버튼, 오답 카드 표시를 테마별로 정리했습니다.
- 모바일 화면에서 버튼이 겹치지 않도록 학습노트 버튼 배치와 입력 영역을 보정했습니다.
- 관리자 맛집 지도 장소 목록 API의 라우트 의존성 누락으로 발생하던 500 오류를 수정했습니다.
- 관리자 사용자 목록과 서비스 DB 상태를 점검해 사용자 데이터가 유지되고 있음을 확인했습니다.
- 배포 전 데이터 보존 절차를 수행하고, 배포 후 백엔드 점검, 프론트엔드 빌드, 주요 URL 응답과 서비스 프로세스 상태를 확인했습니다.
